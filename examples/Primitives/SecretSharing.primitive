Primitive SecretSharing(Set MessageSpace, Set ShareSpace, int n, int t) {
    Set Message = MessageSpace;
    Set Share = ShareSpace;
    int shareCount = n;
    int threshold = t;

    Array<Share, shareCount> Share(Message m);

    Message? Reconstruct(Array<Share, shareCount> s);
}
