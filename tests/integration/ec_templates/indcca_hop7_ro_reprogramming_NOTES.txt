NOTES -- `hop_7_initialize` (and its `decaps` partner): the KDF-PRF challenge
substitution. Measured c256 on `CG_expanded_INDCCA_PQ`. Documentation only.

THIS IS AN EXPORTER BUILD, NOT A FrogLang CHANGE. Earlier cycles recorded it as
BLOCKED-ON-DOUGLAS needing the intermediate game to retain its KDF input. That was
wrong; see "the point is derivable" below.

WHAT THE HOP SAYS
=================
    RD(..., H_c.KDFPRFSec_Random(H)).initialize  ~  GameFreshSS(...).initialize

LEFT   `KDFPRFSec_Random` draws `rF <$ dfun_...` and `lookup rest = rF rest`, so
       the reduction's challenge shared secret is `ss = rF(rest*)`.
RIGHT  `GameFreshSS.Initialize` draws `RF <$ dfun_...` AND an INDEPENDENT
       `ss <- BitString<H.Nout>`.

It is sound because `GameFreshSS.Decaps` returns `None` on `ct == ctStar`, so the
adversary can never observe the random function at the challenge point `rest*`.

THE EMITTED POST IS PROVABLY UNACHIEVABLE -- not merely hard
===========================================================
    ={res} /\ GameFreshSS.rF{2} = H_c.KDFPRFSec_Random.rF{1}

Take any coupling satisfying it. `={res}` forces `ss{2} = ss{1}`, and on the left
`ss{1} = rF{1} rest*`, and full function equality gives `rF{1} = RF{2}`. So in the
joint, `ss{2} = RF{2} rest*` -- a deterministic function of `RF{2}`. But the RIGHT
marginal must be the program's, where `ss{2}` is drawn INDEPENDENTLY of `RF{2}`.
For a non-degenerate output distribution that is a contradiction. **No tactic can
close this lemma; the statement has to change.**

THE REPAIR, AND WHERE THE REPROGRAMMING POINT ACTUALLY LIVES
===========================================================
Weaken the function conjunct from equality to "equal except at the challenge
point", pinned by reprogramming. The point itself is `rest*`, the challenge KDF
input the left's `initialize` builds.

**THE POINT IS NOT DERIVABLE FROM STORED STATE, and an earlier version of these
notes said it was. That claim is withdrawn -- it is false.** Measured on
`CG_expanded_INDCCA_PQ`: the flat states store exactly `pq_keys`, `seed_T`,
`kem_ct`, `ctStar` and the random function, on BOTH sides. `rest*`'s first
component is `ss_T = ElementToSharedSecret(Exp(ek_T, sk_E))`, and `sk_E` is a
LOCAL of `initialize` that nothing stores; its only trace is `ct_T = Exp(g, sk_E)`.
Recovering `ss_T` from `(ek_T, ct_T)` is the Diffie-Hellman problem. The earlier
derivation route -- read `rest*` off the game's own `Decaps`, which computes
`ElementToSharedSecret(Exp(ct_T, dk_T))` -- silently assumed
`Exp(Exp(g,dk),sk) = Exp(Exp(g,sk),dk)`, and NominalGroup.primitive says in as many
words that group-algebraic facts are NOT built in and must be introduced as an
explicit assumption game. This proof assumes only KEMCorrectnessWithDK, KEM_INDCCA
and KDFPRFSec. So the fact is not available.

**IT DOES NOT NEED TO BE DERIVABLE, and that is the actual repair.** `rest*` never
has to appear in the hop's POST. It is a WITNESS, in scope as a local inside
`hop_7_initialize`, where `exists*` can name it. What the post states instead is
the CONSEQUENCE the consumer needs -- that the two random functions agree at every
input `Decaps` can query:

    forall ct, ct <> ctStar => rF{1} (kdfin ct) = rF{2} (kdfin ct)

and `kdfin ct` IS expressible over stored fields, because it is exactly what
`Decaps` computes: `ElementToSharedSecret(Exp(ct_T, dk_T)) || Encode(ct_T) ||
Encode(Exp(Generator(), dk_T)) || L.get()` with `dk_T = RandomScalar(seed_T)`.

THE UNREACHABILITY ARGUMENT IS CLEAN, AND IT NEEDS NO GROUP FACT. Read the two
bodies: `GameFreshSS.Decaps` queries `RF` only in the branch `ct <> ctStar /\
ct_PQ = kem_ct`. In that branch `ct <> ctStar = (kem_ct, ct_T*)` together with
`ct_PQ = kem_ct` FORCES `ct_T <> ct_T*`. `Encode` is declared `deterministic
injective`, so `Encode(ct_T) <> Encode(ct_T*)`, and that component alone separates
`kdfin ct` from `rest*` under concat injectivity. The first components (`ss_T`
against `dec_ss_T`) never have to be related -- which is exactly why no
Diffie-Hellman fact is required.

THE EASYCRYPT INGREDIENT IS A LEMMA, NOT AN AXIOM
=================================================
`MUniFinFun` (EC `theories/distributions/Distr.ec:2552`):

    dlet (dfun d) (fun f => dmap (d x0) (fun y => f.[x0 <- y]))
      = weight (d x0) \cdot dfun d

Drawing the whole function and then REPROGRAMMING ONE POINT with a fresh draw is
distributed as `dfun d` again (the weight is 1 for a lossless `d`). That is exactly
the coupling the hop needs, and c250 already derived this family from
`MUniFinFun`, so no new axiom is involved. Companions if useful:
`dfunE_dlet_fix1`, `dlet_dfun_fupdate_ll`, `dfun1E_fix1`.

Tactic direction: `rnd` the left's single `dfun` draw against the right's TWO
draws (`RF`, then `ss`), coupled by the bijection `f |-> f.[rest* <- ss]`.

THE CONSUMER COST, AND IT MATTERS MORE THAN THE REPAIR
======================================================
`hop_7_decaps` and `hop_15_decaps` were closed in c250 (`_synth_sim_field_rename`)
USING the strong premise `rF{2} = rF{1}` in their precondition. Since that premise
is unachievable, **those lemmas are valid in isolation but cannot be assembled into
the chain** -- the `pr` lemma needs `initialize` to establish what `decaps`
assumes. This is precisely the ORDERING RULE: closing the consumer first looked
like progress and proved nothing.

So weakening the coupling has a price. Under the weaker premise the `decaps` hops
need the UNREACHABILITY argument: the two functions differ only at `rest*`, and
`Decaps` returns `None` on `ct == ctStar`, so no reachable query hits `rest*`. That
is real content -- it needs the guard to imply `rest <> rest*`, which follows from
injectivity of the concat and `ct <> ctStar`, i.e. an `_inj`-family fact on the
KDF-input concat.

**Net: hop 7 is two coupled builds, not one.** Do them together, or the chain does
not close either way.

BUILD ORDER
===========
 1. Emit the reprogramming coupling (`rF{1} = rF{2}.[rest* <- ss]`) with `rest*`
    rendered in `ev_` form from `ctStar`/`seed_T`.
 2. Prove `initialize` with the `dlet_dfun_update` bijection.
 3. Re-prove `decaps` under the weaker premise, discharging `rest <> rest*` from
    the `ct <> ctStar` guard plus concat injectivity. EXPECT the c250 route to stop
    firing here -- its leaf `wp; sim` relies on the strong equality.
 4. Whole-chain check: the `hop_7_pr` lemma must assemble. That is the only
    evidence that step 3 did its job.


*** UPDATE c271: THE MATHEMATICAL CONTENT IS NOW A PROVED EC LEMMA, AND THE
*** REMAINING OBSTRUCTION IS EXACTLY ONE TACTIC-LEVEL QUESTION. ***

Tripwire: `tests/integration/ec_templates/indcca_hop7_reprogram.ec`. It mirrors
the exporter's own shape (`dfn` is `MUF.dfun (fun _ => dcod)` behind an
`op [smt_opaque]`, as `type_collector` emits it) and compiles in ~5s, so it is a
fast loop for this hop. It compiles EXIT 0 with ONLY the equiv admitted; a
one-token corruption of the proved lemma is rejected, so the EXIT 0 is real.

FIRST, THE BODIES, measured on `CG_expanded_INDCCA_PQ` rather than assumed. The
notes above are right that the right side draws `ss` independently, but the
DIFFERENCE IS BIGGER than a sampling difference: the right's `initialize` does not
compute `rest` at all. The left carries FIVE extra deterministic calls
(`NG.exp`, `NG.elementtosharedsecret`, `NG.encode`, `NG.encode`, `L.get`) plus the
`rest <- concat ...` that consumes them. So the tactic has two jobs: functionalize
those five through their `_det` axioms (which is what makes `rest*` writable over
stored state, as the notes predicted), and then the distribution step.

SECOND, THE DISTRIBUTION STEP. `rndsem*` folds each side into ONE draw over pairs:

    left    dmap dfn (fun f => (f, f x0))
    right   dlet dfn (fun g => dmap dcod (fun y => (g, y)))

and the hop's post says the LEFT pair is the RIGHT pair pushed forward through
`h (g, y) = (g.[x0 <- y], y)`. That push-forward is now PROVED as `fold_eq`:

    dlet dfn (fun g => dmap dcod (fun y => (g.[x0 <- y], y)))
  = dmap dfn (fun f => (f, f x0))

by rewriting the pair form as a `dmap` of its first marginal (the second component
is `f x0` on both sides, since `(g.[x0<-y]) x0 = y`) and then applying
`MUniFinFun.dlet_dfun_update` with `weight dcod = 1`. Four lines, no axiom.

THIRD, THE OBSTRUCTION, and it is not "the statement is unachievable" any more --
that was the OLD post. It is that `rnd f finv` demands a BIJECTION between the two
supports and `h` is not injective: the right's `g` carries entropy at `x0` that
the post discards, so every `g` agreeing with `f` off `x0` maps to one left point.
BOTH argument orders were tried and both obligations are false:

    rnd h  id  ->  forall r in dRight, r = h r        (false)
    rnd id h   ->  mu1 dRight l = mu1 dLeft l         (false)

FOURTH, THE NEXT MOVE, which is a tripwire question and not an exporter one. Make
the surplus an EXPLICIT sample so it can be dropped one-sided:

    MUniFinFun.dfunE_dlet_fix1 :  dfun d = dlet (d x0) (fun v => dfun d.[x0 <- dunit v])

splits the right's draw into "the value at x0" and "the rest". The value at `x0`
is then genuinely unused -> one-sided `rnd{2}` under `dcod_ll`. What remains is
the PINNED form, where `(f, y) |-> f.[x0 <- y]` IS injective (`f x0` is known, so
`f` is recoverable) and `dlet_dfun_fupdate_ll` is its distribution identity. That
is a bijection, so `rnd f finv` applies.

A CAUTION EARNED HERE: `cli_step` reported "No open goals" for a `conseq ... => />;
first smt()` that `qed.` then rejected with "cannot prove goal (strict)". The
interactive tool defers smt; only `qed.`/`ec_compile` settles it. Same class as
the known `sim` batch-flakiness, and worth remembering for every `smt`-ended step.

THE CONSUMER COST IS UNCHANGED and still gates the chain: `hop_7_decaps` /
`hop_15_decaps` are closed today against the strong premise `rF{2} = rF{1}`, which
the repaired `initialize` will not establish. They need re-proving under the weak
one with the unreachability argument. Do both or the chain does not close.

THE ROUTE, WORKED OUT ONE STEP FURTHER (still not derived, but no longer vague).
The surplus entropy cannot be removed by rewriting a distribution -- it has to
become a separate PROGRAM STATEMENT before a one-sided `rnd{2}` can drop it. Both
attempts to avoid that failed for the same reason and are worth not repeating:

  * rewriting the RIGHT's folded distribution with `dfunE_dlet_fix1` leaves the
    surplus as a `dlet` INSIDE one sample, which `rnd{2}` cannot reach;
  * rewriting the LEFT's instead, and commuting the right's two independent draws
    so both sides are `dlet dcod (fun v => ...)`, leaves the same non-injective
    map per `v` -- the right's `g` is still unpinned.

So interpose a THIRD program, and the legs split cleanly:

    Mid:  v  <$ dcod;
          rF <$ dfun d.[x0 <- dunit v];
          ss <$ dcod;

  Mid ~ R   is an IDENTITY coupling: fold both with `rndsem*`, and the two
            distributions are equal by `dfunE_dlet_fix1` (that lemma is exactly
            "drawing the value at x0 first, then the pinned function, is drawing
            the function"). `rnd` with the identity bijection.

  L ~ Mid   is a BIJECTIVE coupling, which is the whole point of pinning: with
            `v` in scope, `(g, y) |-> g.[x0 <- y]` is injective on the pinned
            support (`g x0 = v` is known, so `g` is recoverable by resetting
            `x0` to `v`), and `dlet_dfun_fupdate_ll` is its distribution
            identity. Couple the TAIL first -- `v` is a program variable and may
            be read -- and finish with a one-sided `rnd{2}` for the `v` draw
            under `dcod_ll`, which is sound because the post no longer mentions
            it.

Derive that in the tripwire, not in the export.

*** UPDATE c273: THE ARGUMENT IS ASSEMBLED END-TO-END; ONE BOOKKEEPING GOAL LEFT.

`tests/integration/ec_templates/indcca_hop7_reprogram.ec` now compiles EXIT 0 with
ONE `admit`, and `reprogram` -- the hop's own statement -- is PROVED from two legs
by transitivity through a third program. What is proved:

    fold_eq        the unpinned push-forward (from `dlet_dfun_update`)
    fold_eq_pin    the PINNED push-forward   (from `dlet_dfun_fupdate_ll`)
    fupd2, fupd_id, pin_supp, pinR_supp
    leg_mid_r      Mid ~ R, an IDENTITY coupling, COMPLETE
    reprogram      L ~ R, by transitivity through Mid, COMPLETE modulo leg 2

WHY A THIRD PROGRAM IS NEEDED, which is the part that was not obvious: `fold_eq`
alone cannot be USED, because `rnd f finv` demands a support bijection and the
coupling is not injective. Rewriting a distribution does not rescue it either --
`dfunE_dlet_fix1` on EITHER side leaves the surplus as a `dlet` INSIDE one sample,
where a one-sided `rnd{2}` cannot reach it. The surplus must be an explicit
PROGRAM STATEMENT, hence `Mid`, which draws the value at the challenge point FIRST
and then the function PINNED to it. Pinning is what buys injectivity: on the
pinned support `g x0 = v` is known, so `g` is recoverable from `g.[x0 <- y]`.

WHAT IS OPEN: leg 2's three `rnd` obligations. They carry NO distribution content
-- all of that is in `fold_eq_pin` -- and are function-update collapses over
`pinR_supp`. Branch 1 is discharged (`by rewrite fupd2 -(pinR_supp v0 r hr)
fupd_id /#`). Branch 2, the local-injectivity side condition of `in_dmap1E_can`,
is where three spellings failed, all at the `-(pinR_supp v0 y hy)` rewrite: the
preceding `fupd2` collapses the very subterm the `-h1` and `-(pinR_supp)` rewrites
want, so the ORDER is wrong, not the ingredients.

THE MISTAKE TO NOT REPEAT: those three spellings were guessed against a compile
instead of read off the goal, and the compiler's line/column was misattributed to
branch 3 for two rounds. PRINT THE GOAL AT THE FAILING BRANCH FIRST. And note
`cli_step` said "No open goals" for the very tactic `ec_compile` rejects -- the
third time this run that the interactive driver was optimistic; its bullet
structure does not transfer to batch either.

*** UPDATE c274: THE REPROGRAMMING STEP IS PROVED; THE PLAN OF RECORD IS CORRECTED
*** IN BOTH DIRECTIONS. ***

PROVED. `tests/integration/ec_templates/indcca_hop7_reprogram.ec` now compiles
EXIT 0 with ZERO admits and no new axiom. `reprogram` -- the statement
`hop_7_initialize` needs -- is proved from two legs through an interposed `Mid`
that draws the value at the challenge point FIRST and then the function PINNED to
it. Pinning is what buys injectivity, which is what `rnd f finv` requires and what
the un-pinned coupling cannot give.

CORRECTED, DIRECTION 1 (pessimistic): the point is NOT derivable from stored
state -- see the section above, which replaces the claim that it is.

CORRECTED, DIRECTION 2 (optimistic): that does not block the hop, because the
point does not belong in the post at all. It is an internal witness; the post
carries the reachable-agreement consequence, which IS expressible. And the
unreachability that justifies it needs only `Encode` injectivity plus concat
injectivity -- no group fact, no new assumption.

SO THE EXPORTER BUILD IS: emit `hop_7_initialize` as
  (a) functionalize the left's five extra deterministic calls through their `_det`
      axioms, so the challenge KDF input is a named term;
  (b) `exists*` that term and run the Mid transitivity above;
  (c) discharge the post's reachable-agreement conjunct from `Encode`'s `_inj`
      axiom and the branch fact `ct_PQ = kem_ct /\ ct <> ctStar => ct_T <> ct_T*`;
and simultaneously re-state `hop_7_decaps` / `hop_15_decaps` to CONSUME that
conjunct instead of the strong `rF{2} = rF{1}` they assume today. The ordering rule
says both together: the consumers are closed against a premise the repaired
establishing hop will not produce.

NEXT MEASUREMENT BEFORE BUILDING: extend the tripwire with a `Decaps` pair and the
reachable-agreement post, and prove it there. That validates (c) -- the only part
not yet exercised -- at ~5s per iteration instead of ~90s per export.

*** UPDATE c275: THE SEPARATION COSTS NO NEW AXIOM. MEASURED. ***

Part (c) needs "two KDF inputs differing in one component are different". Measured
on the real export of `CG_expanded_INDCCA_PQ`: there is NO concat-injectivity fact
in it (`grep` finds zero), and no `NG_encode_inj` either -- the exporter emits an
`_inj` axiom only when a route REQUESTS one. But it DOES emit, for every concat
triple, the round-trip pair (10 of each in that export):

    slice_concat_left  : slice_L (concat a b) 0 n     = a
    slice_concat_right : slice_R (concat a b) n (n+m) = b

and those give injectivity outright -- apply a slice to both sides of the
hypothesis. `tests/integration/ec_templates/concat_injective.ec` proves it, EXIT 0,
with a negative control (neutering `slice_concat_right`) rejected. It also proves
`concat_neq_right`, the ASYMMETRIC form the separation actually uses: differing in
the RIGHT component suffices and the left components are never related -- which is
the whole point, since in the real hop the left component is the Diffie-Hellman
value nothing relates across the two sides.

So the emission needs: this derived lemma family (no axiom), plus `Encode`'s `_inj`
(licensed, already adjudicated, just needs requesting). `concat_slices_id` is not
needed at all.
