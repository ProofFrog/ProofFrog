Primitive SecretSharing(Set MessageSpace, Set ShareSpace, Int n, Int t) {
    Set Message = MessageSpace;
    Set Shares = ShareSpace;
    Int shareCount = n;
    Int threshold = t;

    Array<Shares, shareCount> Share(Message m);

    Message? Reconstruct(Array<Shares, shareCount> s);
}
